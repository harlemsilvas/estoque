# Vercel Production Environment Variables
# This file configures the app to use static data in Vercel deployment

# Force static data usage (since we don't have a backend API on Vercel)
VITE_USE_STATIC_DATA=true

# API URL (not used when static data is enabled, but kept for reference)
VITE_API_URL=

# Build configuration
VITE_BUILD_TARGET=vercel